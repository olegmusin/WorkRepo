@model IEnumerable<ShiftsViewModel>

<ul id="shiftslist" >
    @foreach (ShiftsViewModel shift in Model)
    {
        <li class="@Html.Raw(shift.IsCanceled ? "hidden" : "") ctrtxt">
            @String.Format("{0:dd/MM/yyyy}", shift.Date)
        </li>
    }

</ul>




