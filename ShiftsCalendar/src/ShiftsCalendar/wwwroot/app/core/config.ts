export let CONFIG = {
    baseUrls: {
        config: "commands/config",
        resetDb: "commands/resetDb",
        projects: "api/projects",
        shifts: "api/projects/${projectId}/shifts",
        workers: "api/workers"
    }
};
